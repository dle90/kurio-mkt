// SUPERSEDED — this script used the retracted account.total_revenue cohort
// method. cohort_and_active.js now produces data/roas-currently-running.csv
// with order-based revenue + Getfly ads_code attribution. Kept as a shim so
// the old path still works; it just runs the canonical script.
import './cohort_and_active.js';
